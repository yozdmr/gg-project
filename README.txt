Instructions to run code:

You will need to install the SpaCy package "en_core_web_sm"

First, run gg_api.py to generate the gg{YEAR}_extracted.json file, which contains our results.
Then run the autograder and other tools to perform analysis on the file.